Cylc Hub now loads `jupyterhub_config.py` files, and Cylc UI Server now loads
`jupyter_cylc_config.py` files. This allows configuration to be targeted at these
components specifically. There has been no change to the directories in which
configuration files may be located.
